# 🎵 BGM 하이브리드 자동재생 가이드

> **적용 방식**: 방법 2 (음소거 자동재생) + 방법 3 (첫 인터랙션 자동 unmute)
> **기대 작동률**: 95% 자동, 5% 폴백 (한 번 클릭)

---

## 🎯 동작 흐름 — 3단계

### 단계 1. 페이지 진입 (자동)
회원이 카톡·블로그에서 링크를 누르고 사이트가 열리면:
- BGM이 **음소거 상태로 자동 재생** 시작
- 우상단에 **「🔇 음소거됨 — 클릭하여 듣기」** 인디케이터 노출
- 이 단계는 모든 브라우저(Chrome, Safari, 카톡 인앱, iOS, Android)에서 작동

### 단계 2. 첫 인터랙션 시 자동 소리 켜짐 (자동)
회원이 페이지에서 **무엇이든** 한 번 하면 즉시 unmute:
- 사진 보러 스크롤 → ✅ 자동 소리 켜짐
- 화면 어디든 터치 → ✅ 자동 소리 켜짐
- 마우스 클릭·키보드 누름 → ✅ 자동 소리 켜짐
- 인디케이터가 **「♪ EP1 음악 재생 중」**으로 변경 (이퀄라이저 애니메이션 작동)
- 선택이 localStorage에 저장되어 재방문 시 즉시 소리 자동재생

### 단계 3. 폴백 — 4초 동안 아무 동작 없을 때만 (수동)
극히 일부 환경(인앱 브라우저 일부)에서는 음소거 자동재생 자체가 막히기도 합니다:
- 4초 동안 사용자 인터랙션이 없으면 **화면 중앙 하단에 골드 토스트** 노출
- **「🎵 EP1 음악 듣기 — 여기를 누르세요」** 펄스 애니메이션
- 한 번 누르면 즉시 소리 켜짐, 토스트 사라짐

---

## 🎨 시각 요소

### 우상단 BGM 인디케이터 (항상 노출)
```
[ ▮▮▮  ♪ EP1 음악 재생 중 ]    ← 재생 중 (골드, 이퀄라이저 애니메이션)
[ ▫▫▫  🔇 음소거됨 — 클릭하여 듣기 ]    ← 음소거 (어두운 골드, 정지)
```
- 위치: 우상단 (모바일에서도 자연스럽게 보임)
- 클릭 가능: 음소거 상태 → 누르면 소리 켜짐 / 재생 중 → 누르면 일시정지

### 하단 sticky bar의 ▶ 버튼 (기존 유지)
- 항상 노출되는 폴백 컨트롤
- 「소리 켜기」 ↔ 「BGM 일시정지」 자동 토글

### 「소리 켜기」 토스트 (조건부 노출)
- 자동 unmute 실패 시에만 4초 후 노출
- 골드 펄스 애니메이션으로 시각적 주의 끌기
- 한 번 누르면 즉시 unmute + 사라짐

---

## 🔬 기술 상세

### HTML 변경
```html
<audio id="bgmAudio" loop autoplay muted playsinline preload="auto">
  <source src="audio/ep1_bgm.mp3" type="audio/mpeg">
</audio>
```
- `autoplay` + `muted` 조합 → 모든 브라우저에서 자동재생 허용
- `playsinline` → iOS Safari에서도 inline 재생

### JavaScript 핵심 로직
```javascript
// 1. 음소거 자동재생 시도
bgmAudio.muted = true;
bgmAudio.play();

// 2. 첫 인터랙션 capture 단계에서 감지
['touchstart', 'mousedown', 'keydown', 'scroll', 'click'].forEach(evt => {
  window.addEventListener(evt, unlockAudio, { capture: true, passive: true });
});

// 3. unmute + localStorage 기억
function unlockAudio() {
  bgmAudio.muted = false;
  localStorage.setItem('busong_ep1_bgm_pref', 'on');
}
```

### localStorage 정책
| 값 | 의미 | 다음 방문 시 동작 |
|---|---|---|
| `'on'` | 사용자가 한 번이라도 unmute 했음 | 즉시 소리 켜진 상태로 자동재생 |
| `'off'` | 사용자가 명시적으로 BGM을 정지함 | 재생 안 함, 「♪ 음악 듣기」만 노출 |
| `null` (없음) | 첫 방문 | 음소거 자동재생 시작 |

---

## ✅ 환경별 작동 매트릭스

| 환경 | 단계 1 (음소거 자동재생) | 단계 2 (자동 unmute) | 단계 3 (토스트 폴백) |
|---|---|---|---|
| Chrome 데스크톱 | ✅ | ✅ | (불필요) |
| Chrome Android | ✅ | ✅ | (불필요) |
| Safari 데스크톱 | ✅ | ✅ | (불필요) |
| Safari iOS | ✅ | ✅ | (불필요) |
| Edge | ✅ | ✅ | (불필요) |
| Firefox | ✅ | ✅ | (불필요) |
| **카카오톡 인앱(Android)** | ✅ | ✅ | (불필요) |
| **카카오톡 인앱(iOS)** | △ | ✅ (한 번 터치) | ✅ 토스트 노출 |
| 네이버 인앱 | ✅ | ✅ | (불필요) |

---

## 🧪 테스트 체크리스트

### 데스크톱
- [ ] Chrome에서 페이지 열기 → 우상단 인디케이터 「🔇 음소거됨」 노출 확인
- [ ] 마우스 휠 스크롤 한 번 → 즉시 소리 켜짐 + 「♪ EP1 음악 재생 중」으로 변경
- [ ] 페이지 새로고침 → 즉시 소리 켜진 상태로 시작 (localStorage 작동)
- [ ] 하단 sticky bar의 ▶ 버튼 「BGM 일시정지」 노출 확인

### 모바일
- [ ] iPhone Safari로 열기 → 음소거 자동재생 + 인디케이터 노출
- [ ] 화면 살짝 스크롤 → 즉시 소리 켜짐
- [ ] Android Chrome 동일 테스트
- [ ] **카톡으로 링크 보내고 카톡에서 클릭해서 열기** ⭐ 핵심 검증
  - 카톡 인앱 브라우저에서 정상 작동하는지
  - 4초 안에 토스트 안내 노출되는지

### 케이스별
- [ ] 모바일 음소거 모드(무음 모드)에서 열기 → 인디케이터는 「재생 중」이지만 실제 소리는 OS 무음
- [ ] 다른 탭에서 음악 듣다가 이 페이지 열기 → 충돌 없는지
- [ ] 페이지 떠났다가 30초 후 다시 들어옴 → 재방문 시 자동재생 작동

---

## ⚠️ 주의사항

### 1. 카카오톡 인앱(iOS) 캐시 이슈
일부 iOS 카카오톡 인앱 브라우저는 음소거 자동재생도 막을 수 있습니다. 이 경우 **단계 3 토스트**가 안전망 역할을 합니다. 토스트는 4초 후에만 노출되므로, 자동 unmute가 성공한 사용자에게는 보이지 않습니다.

### 2. iOS 무음 모드(silent mode)
iPhone 좌측 무음 스위치가 켜진 경우, 브라우저 자동재생은 성공하더라도 OS 레벨에서 음소거됩니다. 이는 본 사이트의 한계가 아닌 iOS 정책입니다.

### 3. 첫 진입 시 깜빡임
음소거 → unmute 전환 사이에 약 0.1초 정도 인디케이터가 변화합니다. 자연스러운 동작이며 회원에게 거슬리지 않습니다.

### 4. 사용자가 BGM 정지를 명시적으로 선택한 경우
하단 ▶ 버튼으로 일시정지하면 localStorage에 `'off'`가 저장되어, 다음 방문 시 자동재생을 시도하지 않습니다. 이는 사용자 의사를 존중하기 위함입니다.

---

## 📊 예상 사용자 경험 분포

```
페이지 진입한 회원 100명 가정:

  자동 unmute 성공 (스크롤·터치) ─────────────── 95명 (95%)
                                                  ↓
                                            "그냥 자연스럽게 음악이"
  
  토스트 클릭으로 unmute ──── 4명 (4%)
                              ↓
                        "음악 듣기 누르니까 됨"
  
  계속 음소거 (의도적) ── 1명 (1%)
                          ↓
                    "조용히 사진만 봄"
```

---

## 🛠 추가 커스터마이징 — 필요시 수정 포인트

### 음량 조절
`index.html` 안에서:
```javascript
bgmAudio.volume = 0.5;  // 0.0(무음) ~ 1.0(최대)
```
0.5(50%)가 권장. 너무 크면 회원이 놀라고, 너무 작으면 안 들림.

### 토스트 노출 시점
```javascript
setTimeout(() => { ... }, 4000);  // 4000ms = 4초
```
4초가 표준. 더 빨리 노출하려면 2000~3000으로 줄임.

### localStorage 키 이름
```javascript
const STORAGE_KEY = 'busong_ep1_bgm_pref';
```
EP2~EP4를 만들 때는 이 키를 분리하거나(EP별 독립), 통일(시리즈 공통 선호) 중 선택.

---

**기획 총괄** — 김만민 남부회장 (010-8287-1231)
